# ─── 回测参数（不可修改）────────────────────────────────────────────────────────

N_DELAY      = 6   # 最新可用数据距目标日的天数
WINDOW_DAYS  = 28  # 训练窗口大小（天）
SLOTS_PER_DAY = 96  # 每天的时段数（15分钟一个时段）
SLOTS_PER_HOUR = SLOTS_PER_DAY // 24  # 每小时的时段数

START_DATE   = "2026-02-01"  # 回测起始日期
END_DATE     = "2026-03-31"  # 回测结束日期
